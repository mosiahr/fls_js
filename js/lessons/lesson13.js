function generateSubSetArray(array) {
    res = []
    for (let i = 0; i < array.length; i++) {
        const element = array[i]
        // if ()
        console.log()
        res.push([array[i + 1]])
    }
}

generateSubSetArray([1, 2, 3])
