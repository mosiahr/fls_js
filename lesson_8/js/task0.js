export default function task0(text) {
    arithmeticMeanNumbers(3,2,4,5,5,5)
}

const arithmeticMeanNumbers = (...numbers) => {
    console.log(numbers.length);
    for (let i = 0; i < numbers.length; i++) {

    }
}
